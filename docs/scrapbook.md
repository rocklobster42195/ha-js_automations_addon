* Intellisense für Header.
* analoguhr
* cronmaster 3000
* API-Audit: `ha.*` auf weitere sinnvolle Sub-Namespaces prüfen. Kandidaten: `ha.state` (getState, getAttr, getStateValue, states), `ha.area` (getAreas, getEntitiesInArea, getAreasInFloor), `ha.label` (getLabels, getEntitiesWithLabel). Vorbild: `ha.history`, `ha.store`, `ha.mqtt`.
* Log-Viewer: generisches Repeat-Grouping für wiederholte Log-Zeilen (nicht nur exakt gleiche Folgezeile, sondern auch kurze alternierende Muster wie "A, B, A, B, ..."). Idee: Sliding-Window über die letzten paar distinct Zeilen pro Quelle in `log-manager.js`, ab N Wiederholungen zu einer Gruppe mit live wachsendem "×N"-Badge zusammenfassen (aufklappbar für Rohzeilen), statt pro Zeile ein Socket-Event zu emittieren. Rohdaten bleiben im Buffer/`logs.json` erhalten. Auslöser war der HA-Reconnect-Log-Spam (siehe `kernel.js` `handleReconnection`), dort aber an der Quelle gelöst (State-Transition-Logging statt Log-Dedup) — dieses Feature wäre der allgemeine Fall für zukünftige Spam-Quellen.